#include <stdio.h>

int main(int argc, char *argv[]) 
{
    char str[5];
    char a[100];
    int i=0,j;
    
    while(fgets(str, sizeof(str), stdin)){
        if(str>0){
            a[i]=str;
            i++;
        }
        if(str==0){
            for(j=0;j<i)
        if(str==0)
        printf("%s", str);
    }

    return 0;
}
